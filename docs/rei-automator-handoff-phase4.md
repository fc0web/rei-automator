# Rei Automator 開発引き継ぎドキュメント
**日付:** 2026年2月18日  
**現在バージョン:** v0.3.0 → v0.4.0 対応中  
**Phase 1〜3:** 完了  
**Phase 4:** 実装ファイル作成済み・統合待ち

---

## Phase 4 実装状況

### ✅ 作成済みファイル

| ファイル | 状態 |
|---------|------|
| `image-matcher.ts`（マッチングエンジン本体） | **完成・そのまま配置可能** |
| `types-phase4-additions.ts`（型定義追加分） | 統合ガイド付き |
| `parser-phase4-additions.ts`（パーサー追加分） | 統合ガイド付き |
| `runtime-phase4-additions.ts`（ランタイム追加分） | 統合ガイド付き |
| `main-phase4-additions.ts`（IPC追加分） | 統合ガイド付き |
| `preload-phase4-additions.ts`（API追加分） | 統合ガイド付き |
| `index-phase4-additions.html`（UI追加分） | 統合ガイド付き |
| `styles-phase4-additions.css`（スタイル追加分） | 統合ガイド付き |
| `renderer-phase4-additions.ts`（UI制御追加分） | 統合ガイド付き |
| `PHASE4-INTEGRATION-GUIDE.md`（統合手順書） | 完成 |

### 📋 統合作業（未実施）

1. `npm install jimp@0.22.12`
2. 各ファイルへの追記（統合ガイド参照）
3. ビルド＆テスト
4. GitHubへプッシュ

### 新コマンド構文

```
find("template.png")             # テンプレート探索
find("template.png", 0.9)        # 閾値指定
click(found)                     # 探索結果クリック
click(found, 10, -5)             # オフセット付き
dblclick(found) / rightclick(found)
wait_find("dialog.png", 10000)   # 見つかるまで待機
wait_find("dialog.png", 10000, 500)  # 間隔指定
find_click("ok-button.png")      # find + click ショートカット
```

---

## プロジェクト構成（Phase 4追加分）

```
C:\Users\user\rei-automator\
├── src/lib/auto/
│   ├── image-matcher.ts    ← 【Phase 4 新規】テンプレートマッチングエンジン
│   ├── controller.ts
│   ├── windows-backend.ts
│   └── stub-backend.ts
├── templates/              ← 【Phase 4 新規】テンプレート画像保存先
└── (その他は Phase 3 と同一)
```
